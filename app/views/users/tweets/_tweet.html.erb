<%
=begin%>
 
<div id="<%= dom_id(tweet) %>">
  <div class="card container border-primary mb-3">
    <div class="card-header row justify-content-between text-white bg-primary">
      <h5 class="m-0"><%= tweet.title %></h5>
      <div>
        <%= link_to '🔎', users_tweets_path(tweet) %> <% if current_user != nil
        && tweet.user_id == current_user.id %> <%= link_to '🖋',
        edit_users_tweet_path(tweet) %> <%= link_to '🗑',
        users_tweet_path(tweet), data: {turbo_method: "delete",turbo_confirm:
        "Are you sure?" } %> <% end %>
      </div>
    </div>
    <div class="card-body">
      <h6 class="card-subtitle mb-2 text-muted">By: <%= tweet.user.email %></h6>
      <p class="card-text">
        <%= truncate tweet.description, length: 75, separator: ' ' %>
      </p>
      <p class="card-text small">
        Created At: <%= tweet.created_at %>| Updated At: <%= tweet.updated_at %>
      </p>
    </div>
  </div>
</div> 
 
<%
=end%>
